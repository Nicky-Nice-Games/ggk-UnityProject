using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// API wise this script will get the user data from logging in and pass it to the NEWDriver script

/// <summary>
/// This script should be initialized when the user logs in. It should handle any behavior that
/// requires to exist at the time of user log in
/// </summary>
public class LoginHandeler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Present user with login

        // Read in the data

        // Send it into a PlayerInfo data structure

        // Send it to the NEWDriver script
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
